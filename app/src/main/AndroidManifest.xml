<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mymall">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.BaseApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.specialsubject.activity.SpeSubXgActivity"></activity>
        <activity android:name=".ui.home.actviity.ManufacturerActivity" />
        <activity android:name=".ui.home.actviity.BrandXqActivity" />
        <activity android:name=".ui.cart.SubmitActivity" />
        <activity android:name=".ui.home.actviity.NewListActivity" />
        <activity android:name=".ui.home.actviity.TabActivity" />
        <activity android:name=".ui.cart.NewAddressActivity" />
        <activity android:name=".ui.cart.AddressActivity" />
        <activity android:name=".ui.home.actviity.GoodDetailActivity" />
        <activity android:name=".ui.own.activity.OwnLoginActivity" />
        <activity android:name=".ui.own.activity.RegisterActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>